# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to record a versioned change. See `docs/contributing.md`
once it lands for the project release flow.
