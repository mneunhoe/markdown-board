# Markdown grammar (v0 — placeholder)

Placeholder. The versioned grammar specification lands in Phase 1 alongside
`packages/core/src/grammar/`.

Until then, see §6 of the hand-off `plan.md` for the v1 grammar contract:

- Vault layout (`TASKS.md`, `DASHBOARD.md`, `library/`, `archive/TASKS.md`).
- Default profile: bold-prefix tokens `[P*]`, `[Day]`, `[pom:N]`.
- Library document shape (fields, sections, tables).
- Archive entry shape.
- Wiki-style `[[links]]` (new in v1).
- Configurable profiles (`default`, future `obsidian-tasks`).
