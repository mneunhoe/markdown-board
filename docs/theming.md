# Theming

Placeholder. Lands in Phase 3.

Will document the CSS-custom-property palette (`--accent`, surface tokens,
typography) and how to drop a CSS override file in the vault root to theme
the app without a restart.
