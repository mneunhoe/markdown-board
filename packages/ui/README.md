# @markdown-board/ui

Svelte 5 components and views (board, list, library, overview).

This package is a placeholder during Phase 0. The real Svelte scaffold,
theme tokens, and component atoms (`TaskCard`, `Column`, `ProjectPill`,
`PriorityBadge`, `DayChip`, …) land in Phase 1.
