{
  "name": "@markdown-board/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Tauri 2 desktop shell. Wraps @markdown-board/ui with a TauriFileAdapter (Phase 2 slice 2) and a native file watcher (slice 3).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@markdown-board/core": "workspace:*",
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-fs": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tauri-apps/cli": "^2.1.0",
    "@types/node": "^22.9.0",
    "happy-dom": "^15.11.6",
    "svelte": "^5.2.0",
    "svelte-check": "^4.0.9",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.4"
  }
}
