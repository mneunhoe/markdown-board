# @markdown-board/desktop

Tauri 2 desktop shell. Wraps `@markdown-board/ui` with a `TauriFileAdapter`
and native file watcher.

Placeholder during Phase 0. Real Tauri scaffold (`src-tauri/`, Rust side,
codesign + auto-update wiring) lands in Phase 2.
