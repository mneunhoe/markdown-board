{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capabilities granted to the main markdown-board window. Slice 2 adds the fs plugin for the TauriFileAdapter; slice 3 adds watch/unwatch for the native external-change watcher. Runtime scoping to a specific vault root is the picker's job (lands with the open-vault slice).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-read-dir",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "fs:allow-stat",
    "fs:allow-watch",
    "fs:allow-unwatch"
  ]
}
