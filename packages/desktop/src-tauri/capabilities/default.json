{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capabilities granted to the main markdown-board window. Slice 1 only exposes core webview APIs; the TauriFileAdapter slice adds fs + path scopes.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
