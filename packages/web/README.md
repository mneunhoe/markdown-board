# @markdown-board/web

Static SvelteKit app that opens a local vault via the browser's File System
Access API. Deployed as the public web demo.

Placeholder during Phase 0. Real scaffold + FSA adapter (`src/lib/adapters/fsa.ts`)
land in Phase 1.
