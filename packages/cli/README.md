# @markdown-board/cli

Headless task operations against a vault — list, add, resolve, archive — for
scripting and AI-assistant integration.

Deferred to Phase 5+. This directory is reserved so the package name and path
are stable.
